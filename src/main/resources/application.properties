spring.application.name=welearn

# URL de conexão para o banco de dados SQL Server na Azure
spring.datasource.url=jdbc:sqlserver://sqlserver-rm97768.database.windows.net:1433;database=dimdimdb;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;

# Nome de usuário para conectar ao banco (substitua "your_password_here" pela senha real)
spring.datasource.username=admsql@sqlserver-rm97768

# Senha do usuário do banco
spring.datasource.password=devops@Fiap2tds

# Driver do SQL Server
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Outras configurações do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect


springdoc.swagger-ui.path=/docs